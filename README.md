Projeto esta no docker hub, para testa-lo so usar o comando "docker compose up -d" no diretorio do arquivo docker compose;
o endpoint para consumir a API, apos rodar o container: http://localhost/api/pedido/v1/new
